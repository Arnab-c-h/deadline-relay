# Normal application startup requires real connections. No simulation fallback.
RELAY_MODE=live
DEMO_CONFIG_PATH=config.local.json
GITHUB_TOKEN=
NOTION_TOKEN=
GOOGLE_TOKEN_PATH=secrets/google-token.json
# Real OpenAI request interpretation. Secrets remain server-side.
LLM_PROVIDER=openai
LLM_MODEL=gpt-5-mini
LLM_API_KEY=

# Explicit offline simulator. Live mode never falls back to simulated services.
RELAY_MODE=simulated
DEMO_CONFIG_PATH=config.local.json
GITHUB_TOKEN=
NOTION_TOKEN=
GOOGLE_TOKEN_PATH=secrets/google-token.json
# Provider/model selection and its adapter are the next setup checkpoint.
LLM_PROVIDER=
LLM_MODEL=
LLM_API_KEY=
